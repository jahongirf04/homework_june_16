const user_email_pass = require("./category.validator");
const user = require("./user.validator");
const category = require("./category.validator");
const product = require("./products.validator");

module.exports = {
  user_email_pass,
  user,
  category,
  product,
};
